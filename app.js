import {calculate} from './scoring.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=n=>n==null?'No score':Number(n).toLocaleString('en-US',{maximumFractionDigits:2});
const valid=n=>Number.isFinite(n)&&n>=0&&n<=100;
const quality=i=>valid(i.strategy)&&valid(i.brand)?(i.strategy+i.brand)/2:null;
const average=items=>{const scores=items.map(quality).filter(valid);return scores.length?scores.reduce((a,b)=>a+b,0)/scores.length:null;};
const target=document.querySelector('#results'),select=document.querySelector('#window');
function worksheet(i){return i.strategyDimensions?`<details class="worksheet"><summary>Scoring evidence</summary>${i.strategyDimensions.map(d=>`<p><strong>${esc(d.dimension)} · ${d.points}/${d.weight}</strong><br>${esc(d.evidence)}</p>`).join('')}</details>`:'';}
function detail(i){return `<article><h3>${esc(i.name)}</h3><div class="score-pair"><span>Branding<b>${fmt(i.brand)}</b></span><span>Strategy<b>${fmt(i.strategy)}</b></span></div><p class="muted">${esc(i.status)}</p><h4>AI recommendation</h4><p>${esc(i.recommendation)}</p>${i.publicationHold?`<p><strong>Review hold:</strong> ${esc(i.publicationHold)}</p>`:''}${worksheet(i)}${i.url?`<a href="${esc(i.url)}" target="_blank" rel="noopener">Source ↗</a>`:''}</article>`;}
function render(r){
 const s=calculate(r),groups=r.surfaces.filter(g=>g.type!=='Inventory coverage'),inventory=r.surfaces.filter(g=>g.type==='Inventory coverage');
 const content=groups.flatMap(g=>g.items.filter(i=>i.kind==='weekly-content').map(i=>({...i,surface:g.name})));
 const surfaces=groups.map(g=>({...g,items:g.items.filter(i=>i.kind!=='weekly-content')}));
 const persistent=surfaces.flatMap(g=>g.items);const entries=[];
 function card(name,items,index,subtitle=''){const id=entries.push({name,items})-1;return `<button class="score-card color-${index%4}" data-detail="${id}"><span>${esc(name)}</span><strong>${fmt(average(items))}</strong><small>${esc(subtitle||'View brand, strategy & recommendation')}</small></button>`;}
 target.innerHTML=`<p class="run-note"><strong>Provisional run</strong> · September 3–9. Missing evidence is marked “No score.” <a href="reports/benchmark-run-2026-09-10-v0.3.md">Coverage & evidence</a></p>
 <section class="score-section"><h2>Surface scores</h2><div class="score-grid">${surfaces.map((g,i)=>card(g.name,g.items,i)).join('')}</div><p class="average">Surface average <strong>${fmt(average(persistent))}</strong></p><details class="inventory"><summary>Additional registered surfaces · review pending</summary><p>These entries need artifact collection and applicability review; they have no score.</p>${inventory.flatMap(g=>g.items).map(i=>`<p>${esc(i.name)} — No score</p>`).join('')}</details></section>
 <section class="score-section"><h2>Content scores</h2><p class="muted">This week’s content · ${content.length} items returned; publishing inventory incomplete.</p><div class="score-grid">${content.map((i,n)=>card(i.name,[i],n,i.surface)).join('')||'<p>No content collected.</p>'}</div><p class="average">Content average <strong>${fmt(average(content))}</strong></p></section>
 <section class="score-section"><h2>Analytics score</h2><div class="analytics-list">${s.analytics.map(k=>`<div class="analytics-row"><div><h3>${esc(k.label)}</h3><p>${k.value===null?'No verified count':fmt(k.value)+' '+esc(k.unit)} · ${esc(k.formula)}</p><small>${esc(k.coverage)}</small></div><strong>${fmt(k.points)}${k.points!==null?'<small>points</small>':''}</strong></div>`).join('')}</div></section>
 <section class="score-section"><h2>2 AI recommendations</h2><ol class="recommendations"><li><strong>Make the current IC strategy consistent across the website.</strong><p>Use the homepage’s systems positioning to update older general consulting copy. Correct the unrelated retreat/refund text on Owners & Developers before driving traffic there.</p></li><li><strong>Complete the evidence the weekly agent needs.</strong><p>Collect branded creative and linked content, reconcile all weekly publications, and fix VM2 dates and participant coverage. Then score branding and qualified people from verified inputs.</p></li></ol></section>
 <section class="total"><div><p class="eyebrow">Weekly total · provisional</p><h2>Observed points</h2><p>Quality and qualified-person points unavailable. Analytics cover only the dates shown above.</p></div><div class="total-number">${fmt(s.total)}<small>points</small></div></section>
 <details class="method"><summary>How the score works</summary><p>Item quality = (strategy + branding) ÷ 2. Surface and content averages summarize assessed items in each section. The combined quality component averages all scored items equally, counting each once.</p><p><strong>Total = combined quality + key people × 5 + YouTube hours × 5 + website views ÷ 20 × 5.</strong> Fractions earn proportional points. Missing components are excluded from the observed subtotal, never scored zero. The total is uncapped.</p><p>${esc(r.methodNote)}</p><p>The full registered inventory and weekly publications are not verified. The separate section averages are not added together. Do not compare incomplete periods as progress.</p><p><a href="benchmarks/strategy_marketing_consistency.md">Strategy rubric</a> · <a href="benchmarks/brand_consistency_benchmark.md">Brand rubric</a> · <a href="benchmarks/ADL%20Brand%20Guide%20v0.1.md">Brand guide</a> · <a href="benchmarks/brand_surfaces.md">Surface register</a> · <a href="data/points-runs.json">Results JSON</a></p></details>
 <dialog aria-labelledby="detail-title"><div class="dialog-heading"><h2 id="detail-title"></h2><button class="close-dialog" aria-label="Close details">Close ×</button></div><div id="detail-body"></div></dialog>`;
 const dialog=target.querySelector('dialog');target.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>{const e=entries[Number(b.dataset.detail)];document.querySelector('#detail-title').textContent=e.name;document.querySelector('#detail-body').innerHTML=e.items.map(detail).join('');dialog.showModal();});target.querySelector('.close-dialog').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog){const b=dialog.getBoundingClientRect();if(e.clientX<b.left||e.clientX>b.right||e.clientY<b.top||e.clientY>b.bottom)dialog.close();}});
}
async function init(){try{const response=await fetch('data/points-runs.json',{cache:'no-store'});if(!response.ok)throw Error();const data=await response.json();select.innerHTML=data.runs.map((r,i)=>`<option value="${i}">${esc(r.label)}</option>`).join('');select.disabled=false;select.onchange=()=>render(data.runs[Number(select.value)]);render(data.runs[0]);}catch{target.innerHTML='<p>Results could not load. No score is displayed without source data.</p><button id="retry">Retry</button>';document.querySelector('#retry').onclick=init;}}init();
